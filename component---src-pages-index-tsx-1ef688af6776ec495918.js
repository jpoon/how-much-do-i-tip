webpackJsonp([221374088121123],{

/***/ 312:
/***/ (function(module, exports) {

	module.exports = [{"name":"Afghanistan","countryCode":"AF"},{"name":"Åland Islands","countryCode":"AX"},{"name":"Albania","countryCode":"AL"},{"name":"Algeria","countryCode":"DZ"},{"name":"American Samoa","countryCode":"AS"},{"name":"Andorra","countryCode":"AD"},{"name":"Angola","countryCode":"AO"},{"name":"Anguilla","countryCode":"AI"},{"name":"Antarctica","countryCode":"AQ"},{"name":"Antigua and Barbuda","countryCode":"AG"},{"name":"Argentina","countryCode":"AR"},{"name":"Armenia","countryCode":"AM"},{"name":"Aruba","countryCode":"AW"},{"name":"Australia","countryCode":"AU"},{"name":"Austria","countryCode":"AT"},{"name":"Azerbaijan","countryCode":"AZ"},{"name":"Bahamas","countryCode":"BS"},{"name":"Bahrain","countryCode":"BH"},{"name":"Bangladesh","countryCode":"BD"},{"name":"Barbados","countryCode":"BB"},{"name":"Belarus","countryCode":"BY"},{"name":"Belgium","countryCode":"BE"},{"name":"Belize","countryCode":"BZ"},{"name":"Benin","countryCode":"BJ"},{"name":"Bermuda","countryCode":"BM"},{"name":"Bhutan","countryCode":"BT"},{"name":"Bolivia (Plurinational State of)","countryCode":"BO"},{"name":"Bonaire, Sint Eustatius and Saba","countryCode":"BQ"},{"name":"Bosnia and Herzegovina","countryCode":"BA"},{"name":"Botswana","countryCode":"BW"},{"name":"Bouvet Island","countryCode":"BV"},{"name":"Brazil","countryCode":"BR"},{"name":"British Indian Ocean Territory","countryCode":"IO"},{"name":"United States Minor Outlying Islands","countryCode":"UM"},{"name":"Virgin Islands (British)","countryCode":"VG"},{"name":"Virgin Islands (U.S.)","countryCode":"VI"},{"name":"Brunei Darussalam","countryCode":"BN"},{"name":"Bulgaria","countryCode":"BG"},{"name":"Burkina Faso","countryCode":"BF"},{"name":"Burundi","countryCode":"BI"},{"name":"Cambodia","countryCode":"KH"},{"name":"Cameroon","countryCode":"CM"},{"name":"Canada","countryCode":"CA","tip":{"default":"15%"}},{"name":"Cabo Verde","countryCode":"CV"},{"name":"Cayman Islands","countryCode":"KY"},{"name":"Central African Republic","countryCode":"CF"},{"name":"Chad","countryCode":"TD"},{"name":"Chile","countryCode":"CL"},{"name":"China","countryCode":"CN"},{"name":"Christmas Island","countryCode":"CX"},{"name":"Cocos (Keeling) Islands","countryCode":"CC"},{"name":"Colombia","countryCode":"CO"},{"name":"Comoros","countryCode":"KM"},{"name":"Congo","countryCode":"CG"},{"name":"Congo (Democratic Republic of the)","countryCode":"CD"},{"name":"Cook Islands","countryCode":"CK"},{"name":"Costa Rica","countryCode":"CR"},{"name":"Croatia","countryCode":"HR"},{"name":"Cuba","countryCode":"CU"},{"name":"Curaçao","countryCode":"CW"},{"name":"Cyprus","countryCode":"CY"},{"name":"Czech Republic","countryCode":"CZ"},{"name":"Denmark","countryCode":"DK"},{"name":"Djibouti","countryCode":"DJ"},{"name":"Dominica","countryCode":"DM"},{"name":"Dominican Republic","countryCode":"DO"},{"name":"Ecuador","countryCode":"EC"},{"name":"Egypt","countryCode":"EG"},{"name":"El Salvador","countryCode":"SV"},{"name":"Equatorial Guinea","countryCode":"GQ"},{"name":"Eritrea","countryCode":"ER"},{"name":"Estonia","countryCode":"EE"},{"name":"Ethiopia","countryCode":"ET"},{"name":"Falkland Islands (Malvinas)","countryCode":"FK"},{"name":"Faroe Islands","countryCode":"FO"},{"name":"Fiji","countryCode":"FJ"},{"name":"Finland","countryCode":"FI"},{"name":"France","countryCode":"FR","tip":{"default":"not necessary, but appreciated","restaurant":"5%","taxi":"10%"}},{"name":"French Guiana","countryCode":"GF"},{"name":"French Polynesia","countryCode":"PF"},{"name":"French Southern Territories","countryCode":"TF"},{"name":"Gabon","countryCode":"GA"},{"name":"Gambia","countryCode":"GM"},{"name":"Georgia","countryCode":"GE"},{"name":"Germany","countryCode":"DE"},{"name":"Ghana","countryCode":"GH"},{"name":"Gibraltar","countryCode":"GI"},{"name":"Greece","countryCode":"GR"},{"name":"Greenland","countryCode":"GL"},{"name":"Grenada","countryCode":"GD"},{"name":"Guadeloupe","countryCode":"GP"},{"name":"Guam","countryCode":"GU"},{"name":"Guatemala","countryCode":"GT"},{"name":"Guernsey","countryCode":"GG"},{"name":"Guinea","countryCode":"GN"},{"name":"Guinea-Bissau","countryCode":"GW"},{"name":"Guyana","countryCode":"GY"},{"name":"Haiti","countryCode":"HT"},{"name":"Heard Island and McDonald Islands","countryCode":"HM"},{"name":"Holy See","countryCode":"VA"},{"name":"Honduras","countryCode":"HN"},{"name":"Hong Kong","countryCode":"HK"},{"name":"Hungary","countryCode":"HU"},{"name":"Iceland","countryCode":"IS"},{"name":"India","countryCode":"IN"},{"name":"Indonesia","countryCode":"ID"},{"name":"Côte d'Ivoire","countryCode":"CI"},{"name":"Iran (Islamic Republic of)","countryCode":"IR"},{"name":"Iraq","countryCode":"IQ"},{"name":"Ireland","countryCode":"IE"},{"name":"Isle of Man","countryCode":"IM"},{"name":"Israel","countryCode":"IL"},{"name":"Italy","countryCode":"IT"},{"name":"Jamaica","countryCode":"JM"},{"name":"Japan","countryCode":"JP"},{"name":"Jersey","countryCode":"JE"},{"name":"Jordan","countryCode":"JO"},{"name":"Kazakhstan","countryCode":"KZ"},{"name":"Kenya","countryCode":"KE"},{"name":"Kiribati","countryCode":"KI"},{"name":"Kuwait","countryCode":"KW"},{"name":"Kyrgyzstan","countryCode":"KG"},{"name":"Lao People's Democratic Republic","countryCode":"LA"},{"name":"Latvia","countryCode":"LV"},{"name":"Lebanon","countryCode":"LB"},{"name":"Lesotho","countryCode":"LS"},{"name":"Liberia","countryCode":"LR"},{"name":"Libya","countryCode":"LY"},{"name":"Liechtenstein","countryCode":"LI"},{"name":"Lithuania","countryCode":"LT"},{"name":"Luxembourg","countryCode":"LU"},{"name":"Macao","countryCode":"MO"},{"name":"Macedonia (the former Yugoslav Republic of)","countryCode":"MK"},{"name":"Madagascar","countryCode":"MG"},{"name":"Malawi","countryCode":"MW"},{"name":"Malaysia","countryCode":"MY"},{"name":"Maldives","countryCode":"MV"},{"name":"Mali","countryCode":"ML"},{"name":"Malta","countryCode":"MT"},{"name":"Marshall Islands","countryCode":"MH"},{"name":"Martinique","countryCode":"MQ"},{"name":"Mauritania","countryCode":"MR"},{"name":"Mauritius","countryCode":"MU"},{"name":"Mayotte","countryCode":"YT"},{"name":"Mexico","countryCode":"MX"},{"name":"Micronesia (Federated States of)","countryCode":"FM"},{"name":"Moldova (Republic of)","countryCode":"MD"},{"name":"Monaco","countryCode":"MC"},{"name":"Mongolia","countryCode":"MN"},{"name":"Montenegro","countryCode":"ME"},{"name":"Montserrat","countryCode":"MS"},{"name":"Morocco","countryCode":"MA"},{"name":"Mozambique","countryCode":"MZ"},{"name":"Myanmar","countryCode":"MM"},{"name":"Namibia","countryCode":"NA"},{"name":"Nauru","countryCode":"NR"},{"name":"Nepal","countryCode":"NP"},{"name":"Netherlands","countryCode":"NL"},{"name":"New Caledonia","countryCode":"NC"},{"name":"New Zealand","countryCode":"NZ"},{"name":"Nicaragua","countryCode":"NI"},{"name":"Niger","countryCode":"NE"},{"name":"Nigeria","countryCode":"NG"},{"name":"Niue","countryCode":"NU"},{"name":"Norfolk Island","countryCode":"NF"},{"name":"Korea (Democratic People's Republic of)","countryCode":"KP"},{"name":"Northern Mariana Islands","countryCode":"MP"},{"name":"Norway","countryCode":"NO"},{"name":"Oman","countryCode":"OM"},{"name":"Pakistan","countryCode":"PK"},{"name":"Palau","countryCode":"PW"},{"name":"Palestine, State of","countryCode":"PS"},{"name":"Panama","countryCode":"PA"},{"name":"Papua New Guinea","countryCode":"PG"},{"name":"Paraguay","countryCode":"PY"},{"name":"Peru","countryCode":"PE"},{"name":"Philippines","countryCode":"PH"},{"name":"Pitcairn","countryCode":"PN"},{"name":"Poland","countryCode":"PL"},{"name":"Portugal","countryCode":"PT"},{"name":"Puerto Rico","countryCode":"PR"},{"name":"Qatar","countryCode":"QA"},{"name":"Republic of Kosovo","countryCode":"XK"},{"name":"Réunion","countryCode":"RE"},{"name":"Romania","countryCode":"RO"},{"name":"Russian Federation","countryCode":"RU"},{"name":"Rwanda","countryCode":"RW"},{"name":"Saint Barthélemy","countryCode":"BL"},{"name":"Saint Helena, Ascension and Tristan da Cunha","countryCode":"SH"},{"name":"Saint Kitts and Nevis","countryCode":"KN"},{"name":"Saint Lucia","countryCode":"LC"},{"name":"Saint Martin (French part)","countryCode":"MF"},{"name":"Saint Pierre and Miquelon","countryCode":"PM"},{"name":"Saint Vincent and the Grenadines","countryCode":"VC"},{"name":"Samoa","countryCode":"WS"},{"name":"San Marino","countryCode":"SM"},{"name":"Sao Tome and Principe","countryCode":"ST"},{"name":"Saudi Arabia","countryCode":"SA"},{"name":"Senegal","countryCode":"SN"},{"name":"Serbia","countryCode":"RS"},{"name":"Seychelles","countryCode":"SC"},{"name":"Sierra Leone","countryCode":"SL"},{"name":"Singapore","countryCode":"SG"},{"name":"Sint Maarten (Dutch part)","countryCode":"SX"},{"name":"Slovakia","countryCode":"SK"},{"name":"Slovenia","countryCode":"SI"},{"name":"Solomon Islands","countryCode":"SB"},{"name":"Somalia","countryCode":"SO"},{"name":"South Africa","countryCode":"ZA"},{"name":"South Georgia and the South Sandwich Islands","countryCode":"GS"},{"name":"Korea (Republic of)","countryCode":"KR"},{"name":"South Sudan","countryCode":"SS"},{"name":"Spain","countryCode":"ES"},{"name":"Sri Lanka","countryCode":"LK"},{"name":"Sudan","countryCode":"SD"},{"name":"Suriname","countryCode":"SR"},{"name":"Svalbard and Jan Mayen","countryCode":"SJ"},{"name":"Swaziland","countryCode":"SZ"},{"name":"Sweden","countryCode":"SE"},{"name":"Switzerland","countryCode":"CH"},{"name":"Syrian Arab Republic","countryCode":"SY"},{"name":"Taiwan","countryCode":"TW"},{"name":"Tajikistan","countryCode":"TJ"},{"name":"Tanzania, United Republic of","countryCode":"TZ"},{"name":"Thailand","countryCode":"TH"},{"name":"Timor-Leste","countryCode":"TL"},{"name":"Togo","countryCode":"TG"},{"name":"Tokelau","countryCode":"TK"},{"name":"Tonga","countryCode":"TO"},{"name":"Trinidad and Tobago","countryCode":"TT"},{"name":"Tunisia","countryCode":"TN"},{"name":"Turkey","countryCode":"TR"},{"name":"Turkmenistan","countryCode":"TM"},{"name":"Turks and Caicos Islands","countryCode":"TC"},{"name":"Tuvalu","countryCode":"TV"},{"name":"Uganda","countryCode":"UG"},{"name":"Ukraine","countryCode":"UA"},{"name":"United Arab Emirates","countryCode":"AE"},{"name":"United Kingdom of Great Britain and Northern Ireland","countryCode":"GB"},{"name":"United States of America","countryCode":"US","tip":{"default":"15%","restaurant":"15%"}},{"name":"Uruguay","countryCode":"UY"},{"name":"Uzbekistan","countryCode":"UZ"},{"name":"Vanuatu","countryCode":"VU"},{"name":"Venezuela (Bolivarian Republic of)","countryCode":"VE"},{"name":"Viet Nam","countryCode":"VN"},{"name":"Wallis and Futuna","countryCode":"WF"},{"name":"Western Sahara","countryCode":"EH"},{"name":"Yemen","countryCode":"YE"},{"name":"Zambia","countryCode":"ZM"},{"name":"Zimbabwe","countryCode":"ZW"}]

/***/ }),

/***/ 92:
/***/ (function(module, exports, __webpack_require__) {

	/**
	 * Copyright (c) 2013-present, Facebook, Inc.
	 *
	 * This source code is licensed under the MIT license found in the
	 * LICENSE file in the root directory of this source tree.
	 *
	 */
	
	'use strict';
	
	var _assign = __webpack_require__(4);
	
	var emptyObject = __webpack_require__(32);
	var _invariant = __webpack_require__(1);
	
	if (false) {
	  var warning = require('fbjs/lib/warning');
	}
	
	var MIXINS_KEY = 'mixins';
	
	// Helper function to allow the creation of anonymous functions which do not
	// have .name set to the name of the variable being assigned to.
	function identity(fn) {
	  return fn;
	}
	
	var ReactPropTypeLocationNames;
	if (false) {
	  ReactPropTypeLocationNames = {
	    prop: 'prop',
	    context: 'context',
	    childContext: 'child context'
	  };
	} else {
	  ReactPropTypeLocationNames = {};
	}
	
	function factory(ReactComponent, isValidElement, ReactNoopUpdateQueue) {
	  /**
	   * Policies that describe methods in `ReactClassInterface`.
	   */
	
	  var injectedMixins = [];
	
	  /**
	   * Composite components are higher-level components that compose other composite
	   * or host components.
	   *
	   * To create a new type of `ReactClass`, pass a specification of
	   * your new class to `React.createClass`. The only requirement of your class
	   * specification is that you implement a `render` method.
	   *
	   *   var MyComponent = React.createClass({
	   *     render: function() {
	   *       return <div>Hello World</div>;
	   *     }
	   *   });
	   *
	   * The class specification supports a specific protocol of methods that have
	   * special meaning (e.g. `render`). See `ReactClassInterface` for
	   * more the comprehensive protocol. Any other properties and methods in the
	   * class specification will be available on the prototype.
	   *
	   * @interface ReactClassInterface
	   * @internal
	   */
	  var ReactClassInterface = {
	    /**
	     * An array of Mixin objects to include when defining your component.
	     *
	     * @type {array}
	     * @optional
	     */
	    mixins: 'DEFINE_MANY',
	
	    /**
	     * An object containing properties and methods that should be defined on
	     * the component's constructor instead of its prototype (static methods).
	     *
	     * @type {object}
	     * @optional
	     */
	    statics: 'DEFINE_MANY',
	
	    /**
	     * Definition of prop types for this component.
	     *
	     * @type {object}
	     * @optional
	     */
	    propTypes: 'DEFINE_MANY',
	
	    /**
	     * Definition of context types for this component.
	     *
	     * @type {object}
	     * @optional
	     */
	    contextTypes: 'DEFINE_MANY',
	
	    /**
	     * Definition of context types this component sets for its children.
	     *
	     * @type {object}
	     * @optional
	     */
	    childContextTypes: 'DEFINE_MANY',
	
	    // ==== Definition methods ====
	
	    /**
	     * Invoked when the component is mounted. Values in the mapping will be set on
	     * `this.props` if that prop is not specified (i.e. using an `in` check).
	     *
	     * This method is invoked before `getInitialState` and therefore cannot rely
	     * on `this.state` or use `this.setState`.
	     *
	     * @return {object}
	     * @optional
	     */
	    getDefaultProps: 'DEFINE_MANY_MERGED',
	
	    /**
	     * Invoked once before the component is mounted. The return value will be used
	     * as the initial value of `this.state`.
	     *
	     *   getInitialState: function() {
	     *     return {
	     *       isOn: false,
	     *       fooBaz: new BazFoo()
	     *     }
	     *   }
	     *
	     * @return {object}
	     * @optional
	     */
	    getInitialState: 'DEFINE_MANY_MERGED',
	
	    /**
	     * @return {object}
	     * @optional
	     */
	    getChildContext: 'DEFINE_MANY_MERGED',
	
	    /**
	     * Uses props from `this.props` and state from `this.state` to render the
	     * structure of the component.
	     *
	     * No guarantees are made about when or how often this method is invoked, so
	     * it must not have side effects.
	     *
	     *   render: function() {
	     *     var name = this.props.name;
	     *     return <div>Hello, {name}!</div>;
	     *   }
	     *
	     * @return {ReactComponent}
	     * @required
	     */
	    render: 'DEFINE_ONCE',
	
	    // ==== Delegate methods ====
	
	    /**
	     * Invoked when the component is initially created and about to be mounted.
	     * This may have side effects, but any external subscriptions or data created
	     * by this method must be cleaned up in `componentWillUnmount`.
	     *
	     * @optional
	     */
	    componentWillMount: 'DEFINE_MANY',
	
	    /**
	     * Invoked when the component has been mounted and has a DOM representation.
	     * However, there is no guarantee that the DOM node is in the document.
	     *
	     * Use this as an opportunity to operate on the DOM when the component has
	     * been mounted (initialized and rendered) for the first time.
	     *
	     * @param {DOMElement} rootNode DOM element representing the component.
	     * @optional
	     */
	    componentDidMount: 'DEFINE_MANY',
	
	    /**
	     * Invoked before the component receives new props.
	     *
	     * Use this as an opportunity to react to a prop transition by updating the
	     * state using `this.setState`. Current props are accessed via `this.props`.
	     *
	     *   componentWillReceiveProps: function(nextProps, nextContext) {
	     *     this.setState({
	     *       likesIncreasing: nextProps.likeCount > this.props.likeCount
	     *     });
	     *   }
	     *
	     * NOTE: There is no equivalent `componentWillReceiveState`. An incoming prop
	     * transition may cause a state change, but the opposite is not true. If you
	     * need it, you are probably looking for `componentWillUpdate`.
	     *
	     * @param {object} nextProps
	     * @optional
	     */
	    componentWillReceiveProps: 'DEFINE_MANY',
	
	    /**
	     * Invoked while deciding if the component should be updated as a result of
	     * receiving new props, state and/or context.
	     *
	     * Use this as an opportunity to `return false` when you're certain that the
	     * transition to the new props/state/context will not require a component
	     * update.
	     *
	     *   shouldComponentUpdate: function(nextProps, nextState, nextContext) {
	     *     return !equal(nextProps, this.props) ||
	     *       !equal(nextState, this.state) ||
	     *       !equal(nextContext, this.context);
	     *   }
	     *
	     * @param {object} nextProps
	     * @param {?object} nextState
	     * @param {?object} nextContext
	     * @return {boolean} True if the component should update.
	     * @optional
	     */
	    shouldComponentUpdate: 'DEFINE_ONCE',
	
	    /**
	     * Invoked when the component is about to update due to a transition from
	     * `this.props`, `this.state` and `this.context` to `nextProps`, `nextState`
	     * and `nextContext`.
	     *
	     * Use this as an opportunity to perform preparation before an update occurs.
	     *
	     * NOTE: You **cannot** use `this.setState()` in this method.
	     *
	     * @param {object} nextProps
	     * @param {?object} nextState
	     * @param {?object} nextContext
	     * @param {ReactReconcileTransaction} transaction
	     * @optional
	     */
	    componentWillUpdate: 'DEFINE_MANY',
	
	    /**
	     * Invoked when the component's DOM representation has been updated.
	     *
	     * Use this as an opportunity to operate on the DOM when the component has
	     * been updated.
	     *
	     * @param {object} prevProps
	     * @param {?object} prevState
	     * @param {?object} prevContext
	     * @param {DOMElement} rootNode DOM element representing the component.
	     * @optional
	     */
	    componentDidUpdate: 'DEFINE_MANY',
	
	    /**
	     * Invoked when the component is about to be removed from its parent and have
	     * its DOM representation destroyed.
	     *
	     * Use this as an opportunity to deallocate any external resources.
	     *
	     * NOTE: There is no `componentDidUnmount` since your component will have been
	     * destroyed by that point.
	     *
	     * @optional
	     */
	    componentWillUnmount: 'DEFINE_MANY',
	
	    /**
	     * Replacement for (deprecated) `componentWillMount`.
	     *
	     * @optional
	     */
	    UNSAFE_componentWillMount: 'DEFINE_MANY',
	
	    /**
	     * Replacement for (deprecated) `componentWillReceiveProps`.
	     *
	     * @optional
	     */
	    UNSAFE_componentWillReceiveProps: 'DEFINE_MANY',
	
	    /**
	     * Replacement for (deprecated) `componentWillUpdate`.
	     *
	     * @optional
	     */
	    UNSAFE_componentWillUpdate: 'DEFINE_MANY',
	
	    // ==== Advanced methods ====
	
	    /**
	     * Updates the component's currently mounted DOM representation.
	     *
	     * By default, this implements React's rendering and reconciliation algorithm.
	     * Sophisticated clients may wish to override this.
	     *
	     * @param {ReactReconcileTransaction} transaction
	     * @internal
	     * @overridable
	     */
	    updateComponent: 'OVERRIDE_BASE'
	  };
	
	  /**
	   * Similar to ReactClassInterface but for static methods.
	   */
	  var ReactClassStaticInterface = {
	    /**
	     * This method is invoked after a component is instantiated and when it
	     * receives new props. Return an object to update state in response to
	     * prop changes. Return null to indicate no change to state.
	     *
	     * If an object is returned, its keys will be merged into the existing state.
	     *
	     * @return {object || null}
	     * @optional
	     */
	    getDerivedStateFromProps: 'DEFINE_MANY_MERGED'
	  };
	
	  /**
	   * Mapping from class specification keys to special processing functions.
	   *
	   * Although these are declared like instance properties in the specification
	   * when defining classes using `React.createClass`, they are actually static
	   * and are accessible on the constructor instead of the prototype. Despite
	   * being static, they must be defined outside of the "statics" key under
	   * which all other static methods are defined.
	   */
	  var RESERVED_SPEC_KEYS = {
	    displayName: function(Constructor, displayName) {
	      Constructor.displayName = displayName;
	    },
	    mixins: function(Constructor, mixins) {
	      if (mixins) {
	        for (var i = 0; i < mixins.length; i++) {
	          mixSpecIntoComponent(Constructor, mixins[i]);
	        }
	      }
	    },
	    childContextTypes: function(Constructor, childContextTypes) {
	      if (false) {
	        validateTypeDef(Constructor, childContextTypes, 'childContext');
	      }
	      Constructor.childContextTypes = _assign(
	        {},
	        Constructor.childContextTypes,
	        childContextTypes
	      );
	    },
	    contextTypes: function(Constructor, contextTypes) {
	      if (false) {
	        validateTypeDef(Constructor, contextTypes, 'context');
	      }
	      Constructor.contextTypes = _assign(
	        {},
	        Constructor.contextTypes,
	        contextTypes
	      );
	    },
	    /**
	     * Special case getDefaultProps which should move into statics but requires
	     * automatic merging.
	     */
	    getDefaultProps: function(Constructor, getDefaultProps) {
	      if (Constructor.getDefaultProps) {
	        Constructor.getDefaultProps = createMergedResultFunction(
	          Constructor.getDefaultProps,
	          getDefaultProps
	        );
	      } else {
	        Constructor.getDefaultProps = getDefaultProps;
	      }
	    },
	    propTypes: function(Constructor, propTypes) {
	      if (false) {
	        validateTypeDef(Constructor, propTypes, 'prop');
	      }
	      Constructor.propTypes = _assign({}, Constructor.propTypes, propTypes);
	    },
	    statics: function(Constructor, statics) {
	      mixStaticSpecIntoComponent(Constructor, statics);
	    },
	    autobind: function() {}
	  };
	
	  function validateTypeDef(Constructor, typeDef, location) {
	    for (var propName in typeDef) {
	      if (typeDef.hasOwnProperty(propName)) {
	        // use a warning instead of an _invariant so components
	        // don't show up in prod but only in __DEV__
	        if (false) {
	          warning(
	            typeof typeDef[propName] === 'function',
	            '%s: %s type `%s` is invalid; it must be a function, usually from ' +
	              'React.PropTypes.',
	            Constructor.displayName || 'ReactClass',
	            ReactPropTypeLocationNames[location],
	            propName
	          );
	        }
	      }
	    }
	  }
	
	  function validateMethodOverride(isAlreadyDefined, name) {
	    var specPolicy = ReactClassInterface.hasOwnProperty(name)
	      ? ReactClassInterface[name]
	      : null;
	
	    // Disallow overriding of base class methods unless explicitly allowed.
	    if (ReactClassMixin.hasOwnProperty(name)) {
	      _invariant(
	        specPolicy === 'OVERRIDE_BASE',
	        'ReactClassInterface: You are attempting to override ' +
	          '`%s` from your class specification. Ensure that your method names ' +
	          'do not overlap with React methods.',
	        name
	      );
	    }
	
	    // Disallow defining methods more than once unless explicitly allowed.
	    if (isAlreadyDefined) {
	      _invariant(
	        specPolicy === 'DEFINE_MANY' || specPolicy === 'DEFINE_MANY_MERGED',
	        'ReactClassInterface: You are attempting to define ' +
	          '`%s` on your component more than once. This conflict may be due ' +
	          'to a mixin.',
	        name
	      );
	    }
	  }
	
	  /**
	   * Mixin helper which handles policy validation and reserved
	   * specification keys when building React classes.
	   */
	  function mixSpecIntoComponent(Constructor, spec) {
	    if (!spec) {
	      if (false) {
	        var typeofSpec = typeof spec;
	        var isMixinValid = typeofSpec === 'object' && spec !== null;
	
	        if (process.env.NODE_ENV !== 'production') {
	          warning(
	            isMixinValid,
	            "%s: You're attempting to include a mixin that is either null " +
	              'or not an object. Check the mixins included by the component, ' +
	              'as well as any mixins they include themselves. ' +
	              'Expected object but got %s.',
	            Constructor.displayName || 'ReactClass',
	            spec === null ? null : typeofSpec
	          );
	        }
	      }
	
	      return;
	    }
	
	    _invariant(
	      typeof spec !== 'function',
	      "ReactClass: You're attempting to " +
	        'use a component class or function as a mixin. Instead, just use a ' +
	        'regular object.'
	    );
	    _invariant(
	      !isValidElement(spec),
	      "ReactClass: You're attempting to " +
	        'use a component as a mixin. Instead, just use a regular object.'
	    );
	
	    var proto = Constructor.prototype;
	    var autoBindPairs = proto.__reactAutoBindPairs;
	
	    // By handling mixins before any other properties, we ensure the same
	    // chaining order is applied to methods with DEFINE_MANY policy, whether
	    // mixins are listed before or after these methods in the spec.
	    if (spec.hasOwnProperty(MIXINS_KEY)) {
	      RESERVED_SPEC_KEYS.mixins(Constructor, spec.mixins);
	    }
	
	    for (var name in spec) {
	      if (!spec.hasOwnProperty(name)) {
	        continue;
	      }
	
	      if (name === MIXINS_KEY) {
	        // We have already handled mixins in a special case above.
	        continue;
	      }
	
	      var property = spec[name];
	      var isAlreadyDefined = proto.hasOwnProperty(name);
	      validateMethodOverride(isAlreadyDefined, name);
	
	      if (RESERVED_SPEC_KEYS.hasOwnProperty(name)) {
	        RESERVED_SPEC_KEYS[name](Constructor, property);
	      } else {
	        // Setup methods on prototype:
	        // The following member methods should not be automatically bound:
	        // 1. Expected ReactClass methods (in the "interface").
	        // 2. Overridden methods (that were mixed in).
	        var isReactClassMethod = ReactClassInterface.hasOwnProperty(name);
	        var isFunction = typeof property === 'function';
	        var shouldAutoBind =
	          isFunction &&
	          !isReactClassMethod &&
	          !isAlreadyDefined &&
	          spec.autobind !== false;
	
	        if (shouldAutoBind) {
	          autoBindPairs.push(name, property);
	          proto[name] = property;
	        } else {
	          if (isAlreadyDefined) {
	            var specPolicy = ReactClassInterface[name];
	
	            // These cases should already be caught by validateMethodOverride.
	            _invariant(
	              isReactClassMethod &&
	                (specPolicy === 'DEFINE_MANY_MERGED' ||
	                  specPolicy === 'DEFINE_MANY'),
	              'ReactClass: Unexpected spec policy %s for key %s ' +
	                'when mixing in component specs.',
	              specPolicy,
	              name
	            );
	
	            // For methods which are defined more than once, call the existing
	            // methods before calling the new property, merging if appropriate.
	            if (specPolicy === 'DEFINE_MANY_MERGED') {
	              proto[name] = createMergedResultFunction(proto[name], property);
	            } else if (specPolicy === 'DEFINE_MANY') {
	              proto[name] = createChainedFunction(proto[name], property);
	            }
	          } else {
	            proto[name] = property;
	            if (false) {
	              // Add verbose displayName to the function, which helps when looking
	              // at profiling tools.
	              if (typeof property === 'function' && spec.displayName) {
	                proto[name].displayName = spec.displayName + '_' + name;
	              }
	            }
	          }
	        }
	      }
	    }
	  }
	
	  function mixStaticSpecIntoComponent(Constructor, statics) {
	    if (!statics) {
	      return;
	    }
	
	    for (var name in statics) {
	      var property = statics[name];
	      if (!statics.hasOwnProperty(name)) {
	        continue;
	      }
	
	      var isReserved = name in RESERVED_SPEC_KEYS;
	      _invariant(
	        !isReserved,
	        'ReactClass: You are attempting to define a reserved ' +
	          'property, `%s`, that shouldn\'t be on the "statics" key. Define it ' +
	          'as an instance property instead; it will still be accessible on the ' +
	          'constructor.',
	        name
	      );
	
	      var isAlreadyDefined = name in Constructor;
	      if (isAlreadyDefined) {
	        var specPolicy = ReactClassStaticInterface.hasOwnProperty(name)
	          ? ReactClassStaticInterface[name]
	          : null;
	
	        _invariant(
	          specPolicy === 'DEFINE_MANY_MERGED',
	          'ReactClass: You are attempting to define ' +
	            '`%s` on your component more than once. This conflict may be ' +
	            'due to a mixin.',
	          name
	        );
	
	        Constructor[name] = createMergedResultFunction(Constructor[name], property);
	
	        return;
	      }
	
	      Constructor[name] = property;
	    }
	  }
	
	  /**
	   * Merge two objects, but throw if both contain the same key.
	   *
	   * @param {object} one The first object, which is mutated.
	   * @param {object} two The second object
	   * @return {object} one after it has been mutated to contain everything in two.
	   */
	  function mergeIntoWithNoDuplicateKeys(one, two) {
	    _invariant(
	      one && two && typeof one === 'object' && typeof two === 'object',
	      'mergeIntoWithNoDuplicateKeys(): Cannot merge non-objects.'
	    );
	
	    for (var key in two) {
	      if (two.hasOwnProperty(key)) {
	        _invariant(
	          one[key] === undefined,
	          'mergeIntoWithNoDuplicateKeys(): ' +
	            'Tried to merge two objects with the same key: `%s`. This conflict ' +
	            'may be due to a mixin; in particular, this may be caused by two ' +
	            'getInitialState() or getDefaultProps() methods returning objects ' +
	            'with clashing keys.',
	          key
	        );
	        one[key] = two[key];
	      }
	    }
	    return one;
	  }
	
	  /**
	   * Creates a function that invokes two functions and merges their return values.
	   *
	   * @param {function} one Function to invoke first.
	   * @param {function} two Function to invoke second.
	   * @return {function} Function that invokes the two argument functions.
	   * @private
	   */
	  function createMergedResultFunction(one, two) {
	    return function mergedResult() {
	      var a = one.apply(this, arguments);
	      var b = two.apply(this, arguments);
	      if (a == null) {
	        return b;
	      } else if (b == null) {
	        return a;
	      }
	      var c = {};
	      mergeIntoWithNoDuplicateKeys(c, a);
	      mergeIntoWithNoDuplicateKeys(c, b);
	      return c;
	    };
	  }
	
	  /**
	   * Creates a function that invokes two functions and ignores their return vales.
	   *
	   * @param {function} one Function to invoke first.
	   * @param {function} two Function to invoke second.
	   * @return {function} Function that invokes the two argument functions.
	   * @private
	   */
	  function createChainedFunction(one, two) {
	    return function chainedFunction() {
	      one.apply(this, arguments);
	      two.apply(this, arguments);
	    };
	  }
	
	  /**
	   * Binds a method to the component.
	   *
	   * @param {object} component Component whose method is going to be bound.
	   * @param {function} method Method to be bound.
	   * @return {function} The bound method.
	   */
	  function bindAutoBindMethod(component, method) {
	    var boundMethod = method.bind(component);
	    if (false) {
	      boundMethod.__reactBoundContext = component;
	      boundMethod.__reactBoundMethod = method;
	      boundMethod.__reactBoundArguments = null;
	      var componentName = component.constructor.displayName;
	      var _bind = boundMethod.bind;
	      boundMethod.bind = function(newThis) {
	        for (
	          var _len = arguments.length,
	            args = Array(_len > 1 ? _len - 1 : 0),
	            _key = 1;
	          _key < _len;
	          _key++
	        ) {
	          args[_key - 1] = arguments[_key];
	        }
	
	        // User is trying to bind() an autobound method; we effectively will
	        // ignore the value of "this" that the user is trying to use, so
	        // let's warn.
	        if (newThis !== component && newThis !== null) {
	          if (process.env.NODE_ENV !== 'production') {
	            warning(
	              false,
	              'bind(): React component methods may only be bound to the ' +
	                'component instance. See %s',
	              componentName
	            );
	          }
	        } else if (!args.length) {
	          if (process.env.NODE_ENV !== 'production') {
	            warning(
	              false,
	              'bind(): You are binding a component method to the component. ' +
	                'React does this for you automatically in a high-performance ' +
	                'way, so you can safely remove this call. See %s',
	              componentName
	            );
	          }
	          return boundMethod;
	        }
	        var reboundMethod = _bind.apply(boundMethod, arguments);
	        reboundMethod.__reactBoundContext = component;
	        reboundMethod.__reactBoundMethod = method;
	        reboundMethod.__reactBoundArguments = args;
	        return reboundMethod;
	      };
	    }
	    return boundMethod;
	  }
	
	  /**
	   * Binds all auto-bound methods in a component.
	   *
	   * @param {object} component Component whose method is going to be bound.
	   */
	  function bindAutoBindMethods(component) {
	    var pairs = component.__reactAutoBindPairs;
	    for (var i = 0; i < pairs.length; i += 2) {
	      var autoBindKey = pairs[i];
	      var method = pairs[i + 1];
	      component[autoBindKey] = bindAutoBindMethod(component, method);
	    }
	  }
	
	  var IsMountedPreMixin = {
	    componentDidMount: function() {
	      this.__isMounted = true;
	    }
	  };
	
	  var IsMountedPostMixin = {
	    componentWillUnmount: function() {
	      this.__isMounted = false;
	    }
	  };
	
	  /**
	   * Add more to the ReactClass base class. These are all legacy features and
	   * therefore not already part of the modern ReactComponent.
	   */
	  var ReactClassMixin = {
	    /**
	     * TODO: This will be deprecated because state should always keep a consistent
	     * type signature and the only use case for this, is to avoid that.
	     */
	    replaceState: function(newState, callback) {
	      this.updater.enqueueReplaceState(this, newState, callback);
	    },
	
	    /**
	     * Checks whether or not this composite component is mounted.
	     * @return {boolean} True if mounted, false otherwise.
	     * @protected
	     * @final
	     */
	    isMounted: function() {
	      if (false) {
	        warning(
	          this.__didWarnIsMounted,
	          '%s: isMounted is deprecated. Instead, make sure to clean up ' +
	            'subscriptions and pending requests in componentWillUnmount to ' +
	            'prevent memory leaks.',
	          (this.constructor && this.constructor.displayName) ||
	            this.name ||
	            'Component'
	        );
	        this.__didWarnIsMounted = true;
	      }
	      return !!this.__isMounted;
	    }
	  };
	
	  var ReactClassComponent = function() {};
	  _assign(
	    ReactClassComponent.prototype,
	    ReactComponent.prototype,
	    ReactClassMixin
	  );
	
	  /**
	   * Creates a composite component class given a class specification.
	   * See https://facebook.github.io/react/docs/top-level-api.html#react.createclass
	   *
	   * @param {object} spec Class specification (which must define `render`).
	   * @return {function} Component constructor function.
	   * @public
	   */
	  function createClass(spec) {
	    // To keep our warnings more understandable, we'll use a little hack here to
	    // ensure that Constructor.name !== 'Constructor'. This makes sure we don't
	    // unnecessarily identify a class without displayName as 'Constructor'.
	    var Constructor = identity(function(props, context, updater) {
	      // This constructor gets overridden by mocks. The argument is used
	      // by mocks to assert on what gets mounted.
	
	      if (false) {
	        warning(
	          this instanceof Constructor,
	          'Something is calling a React component directly. Use a factory or ' +
	            'JSX instead. See: https://fb.me/react-legacyfactory'
	        );
	      }
	
	      // Wire up auto-binding
	      if (this.__reactAutoBindPairs.length) {
	        bindAutoBindMethods(this);
	      }
	
	      this.props = props;
	      this.context = context;
	      this.refs = emptyObject;
	      this.updater = updater || ReactNoopUpdateQueue;
	
	      this.state = null;
	
	      // ReactClasses doesn't have constructors. Instead, they use the
	      // getInitialState and componentWillMount methods for initialization.
	
	      var initialState = this.getInitialState ? this.getInitialState() : null;
	      if (false) {
	        // We allow auto-mocks to proceed as if they're returning null.
	        if (
	          initialState === undefined &&
	          this.getInitialState._isMockFunction
	        ) {
	          // This is probably bad practice. Consider warning here and
	          // deprecating this convenience.
	          initialState = null;
	        }
	      }
	      _invariant(
	        typeof initialState === 'object' && !Array.isArray(initialState),
	        '%s.getInitialState(): must return an object or null',
	        Constructor.displayName || 'ReactCompositeComponent'
	      );
	
	      this.state = initialState;
	    });
	    Constructor.prototype = new ReactClassComponent();
	    Constructor.prototype.constructor = Constructor;
	    Constructor.prototype.__reactAutoBindPairs = [];
	
	    injectedMixins.forEach(mixSpecIntoComponent.bind(null, Constructor));
	
	    mixSpecIntoComponent(Constructor, IsMountedPreMixin);
	    mixSpecIntoComponent(Constructor, spec);
	    mixSpecIntoComponent(Constructor, IsMountedPostMixin);
	
	    // Initialize the defaultProps property after all mixins have been merged.
	    if (Constructor.getDefaultProps) {
	      Constructor.defaultProps = Constructor.getDefaultProps();
	    }
	
	    if (false) {
	      // This is a tag to indicate that the use of these method names is ok,
	      // since it's used with createClass. If it's not, then it's likely a
	      // mistake so we'll warn you to use the static property, property
	      // initializer or constructor respectively.
	      if (Constructor.getDefaultProps) {
	        Constructor.getDefaultProps.isReactClassApproved = {};
	      }
	      if (Constructor.prototype.getInitialState) {
	        Constructor.prototype.getInitialState.isReactClassApproved = {};
	      }
	    }
	
	    _invariant(
	      Constructor.prototype.render,
	      'createClass(...): Class specification must implement a `render` method.'
	    );
	
	    if (false) {
	      warning(
	        !Constructor.prototype.componentShouldUpdate,
	        '%s has a method called ' +
	          'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' +
	          'The name is phrased as a question because the function is ' +
	          'expected to return a value.',
	        spec.displayName || 'A component'
	      );
	      warning(
	        !Constructor.prototype.componentWillRecieveProps,
	        '%s has a method called ' +
	          'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?',
	        spec.displayName || 'A component'
	      );
	      warning(
	        !Constructor.prototype.UNSAFE_componentWillRecieveProps,
	        '%s has a method called UNSAFE_componentWillRecieveProps(). ' +
	          'Did you mean UNSAFE_componentWillReceiveProps()?',
	        spec.displayName || 'A component'
	      );
	    }
	
	    // Reduce time spent doing lookups by setting these on the prototype.
	    for (var methodName in ReactClassInterface) {
	      if (!Constructor.prototype[methodName]) {
	        Constructor.prototype[methodName] = null;
	      }
	    }
	
	    return Constructor;
	  }
	
	  return createClass;
	}
	
	module.exports = factory;


/***/ }),

/***/ 270:
/***/ (function(module, exports) {

	'use strict';
	var token = '%[a-f0-9]{2}';
	var singleMatcher = new RegExp(token, 'gi');
	var multiMatcher = new RegExp('(' + token + ')+', 'gi');
	
	function decodeComponents(components, split) {
		try {
			// Try to decode the entire string first
			return decodeURIComponent(components.join(''));
		} catch (err) {
			// Do nothing
		}
	
		if (components.length === 1) {
			return components;
		}
	
		split = split || 1;
	
		// Split the array in 2 parts
		var left = components.slice(0, split);
		var right = components.slice(split);
	
		return Array.prototype.concat.call([], decodeComponents(left), decodeComponents(right));
	}
	
	function decode(input) {
		try {
			return decodeURIComponent(input);
		} catch (err) {
			var tokens = input.match(singleMatcher);
	
			for (var i = 1; i < tokens.length; i++) {
				input = decodeComponents(tokens, i).join('');
	
				tokens = input.match(singleMatcher);
			}
	
			return input;
		}
	}
	
	function customDecodeURIComponent(input) {
		// Keep track of all the replacements and prefill the map with the `BOM`
		var replaceMap = {
			'%FE%FF': '\uFFFD\uFFFD',
			'%FF%FE': '\uFFFD\uFFFD'
		};
	
		var match = multiMatcher.exec(input);
		while (match) {
			try {
				// Decode as big chunks as possible
				replaceMap[match[0]] = decodeURIComponent(match[0]);
			} catch (err) {
				var result = decode(match[0]);
	
				if (result !== match[0]) {
					replaceMap[match[0]] = result;
				}
			}
	
			match = multiMatcher.exec(input);
		}
	
		// Add `%C2` at the end of the map to make sure it does not replace the combinator before everything else
		replaceMap['%C2'] = '\uFFFD';
	
		var entries = Object.keys(replaceMap);
	
		for (var i = 0; i < entries.length; i++) {
			// Replace all decoded components
			var key = entries[i];
			input = input.replace(new RegExp(key, 'g'), replaceMap[key]);
		}
	
		return input;
	}
	
	module.exports = function (encodedURI) {
		if (typeof encodedURI !== 'string') {
			throw new TypeError('Expected `encodedURI` to be of type `string`, got `' + typeof encodedURI + '`');
		}
	
		try {
			encodedURI = encodedURI.replace(/\+/g, ' ');
	
			// Try the built in decoder first
			return decodeURIComponent(encodedURI);
		} catch (err) {
			// Fallback to a more advanced decoder
			return customDecodeURIComponent(encodedURI);
		}
	};


/***/ }),

/***/ 4:
/***/ (function(module, exports) {

	/*
	object-assign
	(c) Sindre Sorhus
	@license MIT
	*/
	
	'use strict';
	/* eslint-disable no-unused-vars */
	var getOwnPropertySymbols = Object.getOwnPropertySymbols;
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	var propIsEnumerable = Object.prototype.propertyIsEnumerable;
	
	function toObject(val) {
		if (val === null || val === undefined) {
			throw new TypeError('Object.assign cannot be called with null or undefined');
		}
	
		return Object(val);
	}
	
	function shouldUseNative() {
		try {
			if (!Object.assign) {
				return false;
			}
	
			// Detect buggy property enumeration order in older V8 versions.
	
			// https://bugs.chromium.org/p/v8/issues/detail?id=4118
			var test1 = new String('abc');  // eslint-disable-line no-new-wrappers
			test1[5] = 'de';
			if (Object.getOwnPropertyNames(test1)[0] === '5') {
				return false;
			}
	
			// https://bugs.chromium.org/p/v8/issues/detail?id=3056
			var test2 = {};
			for (var i = 0; i < 10; i++) {
				test2['_' + String.fromCharCode(i)] = i;
			}
			var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
				return test2[n];
			});
			if (order2.join('') !== '0123456789') {
				return false;
			}
	
			// https://bugs.chromium.org/p/v8/issues/detail?id=3056
			var test3 = {};
			'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
				test3[letter] = letter;
			});
			if (Object.keys(Object.assign({}, test3)).join('') !==
					'abcdefghijklmnopqrst') {
				return false;
			}
	
			return true;
		} catch (err) {
			// We don't expect any of the above to throw, but better to be safe.
			return false;
		}
	}
	
	module.exports = shouldUseNative() ? Object.assign : function (target, source) {
		var from;
		var to = toObject(target);
		var symbols;
	
		for (var s = 1; s < arguments.length; s++) {
			from = Object(arguments[s]);
	
			for (var key in from) {
				if (hasOwnProperty.call(from, key)) {
					to[key] = from[key];
				}
			}
	
			if (getOwnPropertySymbols) {
				symbols = getOwnPropertySymbols(from);
				for (var i = 0; i < symbols.length; i++) {
					if (propIsEnumerable.call(from, symbols[i])) {
						to[symbols[i]] = from[symbols[i]];
					}
				}
			}
		}
	
		return to;
	};


/***/ }),

/***/ 317:
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	const strictUriEncode = __webpack_require__(418);
	const decodeComponent = __webpack_require__(270);
	
	function encoderForArrayFormat(options) {
		switch (options.arrayFormat) {
			case 'index':
				return (key, value, index) => {
					return value === null ? [
						encode(key, options),
						'[',
						index,
						']'
					].join('') : [
						encode(key, options),
						'[',
						encode(index, options),
						']=',
						encode(value, options)
					].join('');
				};
			case 'bracket':
				return (key, value) => {
					return value === null ? [encode(key, options), '[]'].join('') : [
						encode(key, options),
						'[]=',
						encode(value, options)
					].join('');
				};
			default:
				return (key, value) => {
					return value === null ? encode(key, options) : [
						encode(key, options),
						'=',
						encode(value, options)
					].join('');
				};
		}
	}
	
	function parserForArrayFormat(options) {
		let result;
	
		switch (options.arrayFormat) {
			case 'index':
				return (key, value, accumulator) => {
					result = /\[(\d*)\]$/.exec(key);
	
					key = key.replace(/\[\d*\]$/, '');
	
					if (!result) {
						accumulator[key] = value;
						return;
					}
	
					if (accumulator[key] === undefined) {
						accumulator[key] = {};
					}
	
					accumulator[key][result[1]] = value;
				};
			case 'bracket':
				return (key, value, accumulator) => {
					result = /(\[\])$/.exec(key);
					key = key.replace(/\[\]$/, '');
	
					if (!result) {
						accumulator[key] = value;
						return;
					}
	
					if (accumulator[key] === undefined) {
						accumulator[key] = [value];
						return;
					}
	
					accumulator[key] = [].concat(accumulator[key], value);
				};
			default:
				return (key, value, accumulator) => {
					if (accumulator[key] === undefined) {
						accumulator[key] = value;
						return;
					}
	
					accumulator[key] = [].concat(accumulator[key], value);
				};
		}
	}
	
	function encode(value, options) {
		if (options.encode) {
			return options.strict ? strictUriEncode(value) : encodeURIComponent(value);
		}
	
		return value;
	}
	
	function decode(value, options) {
		if (options.decode) {
			return decodeComponent(value);
		}
	
		return value;
	}
	
	function keysSorter(input) {
		if (Array.isArray(input)) {
			return input.sort();
		}
	
		if (typeof input === 'object') {
			return keysSorter(Object.keys(input))
				.sort((a, b) => Number(a) - Number(b))
				.map(key => input[key]);
		}
	
		return input;
	}
	
	function extract(input) {
		const queryStart = input.indexOf('?');
		if (queryStart === -1) {
			return '';
		}
		return input.slice(queryStart + 1);
	}
	
	function parse(input, options) {
		options = Object.assign({decode: true, arrayFormat: 'none'}, options);
	
		const formatter = parserForArrayFormat(options);
	
		// Create an object with no prototype
		const ret = Object.create(null);
	
		if (typeof input !== 'string') {
			return ret;
		}
	
		input = input.trim().replace(/^[?#&]/, '');
	
		if (!input) {
			return ret;
		}
	
		for (const param of input.split('&')) {
			let [key, value] = param.replace(/\+/g, ' ').split('=');
	
			// Missing `=` should be `null`:
			// http://w3.org/TR/2012/WD-url-20120524/#collect-url-parameters
			value = value === undefined ? null : decode(value, options);
	
			formatter(decode(key, options), value, ret);
		}
	
		return Object.keys(ret).sort().reduce((result, key) => {
			const value = ret[key];
			if (Boolean(value) && typeof value === 'object' && !Array.isArray(value)) {
				// Sort object keys, not values
				result[key] = keysSorter(value);
			} else {
				result[key] = value;
			}
	
			return result;
		}, Object.create(null));
	}
	
	exports.extract = extract;
	exports.parse = parse;
	
	exports.stringify = (obj, options) => {
		const defaults = {
			encode: true,
			strict: true,
			arrayFormat: 'none'
		};
	
		options = Object.assign(defaults, options);
	
		if (options.sort === false) {
			options.sort = () => {};
		}
	
		const formatter = encoderForArrayFormat(options);
	
		return obj ? Object.keys(obj).sort(options.sort).map(key => {
			const value = obj[key];
	
			if (value === undefined) {
				return '';
			}
	
			if (value === null) {
				return encode(key, options);
			}
	
			if (Array.isArray(value)) {
				const result = [];
	
				for (const value2 of value.slice()) {
					if (value2 === undefined) {
						continue;
					}
	
					result.push(formatter(key, value2, result.length));
				}
	
				return result.join('&');
			}
	
			return encode(key, options) + '=' + encode(value, options);
		}).filter(x => x.length > 0).join('&') : '';
	};
	
	exports.parseUrl = (input, options) => {
		return {
			url: input.split('?')[0] || '',
			query: parse(extract(input), options)
		};
	};


/***/ }),

/***/ 418:
/***/ (function(module, exports) {

	'use strict';
	module.exports = str => encodeURIComponent(str).replace(/[!'()*]/g, x => `%${x.charCodeAt(0).toString(16).toUpperCase()}`);


/***/ }),

/***/ 194:
/***/ (function(module, exports, __webpack_require__) {

	"use strict";
	
	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
	
	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }
	
	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }
	
	Object.defineProperty(exports, "__esModule", { value: true });
	var React = __webpack_require__(5);
	
	var default_1 = function (_React$Component) {
	    _inherits(default_1, _React$Component);
	
	    function default_1(props) {
	        _classCallCheck(this, default_1);
	
	        var _this = _possibleConstructorReturn(this, _React$Component.call(this, props));
	
	        _this.countryCodeMap = __webpack_require__(312);
	        _this.state = { tip: "" };
	        return _this;
	    }
	
	    default_1.prototype._getTipForCountryCode = function _getTipForCountryCode(countryCode) {
	        var country = this.countryCodeMap.find(function (c) {
	            return c.countryCode === countryCode;
	        });
	        if (!country) {
	            var errorMsg = "Unable to find country with country code: " + countryCode;
	            console.error(errorMsg);
	            this.setState({ error: errorMsg });
	            return;
	        }
	        this.setState({ countryName: country.name, tip: country.tip });
	    };
	
	    default_1.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
	        if (this.props.countryCode != nextProps.countryCode) {
	            this._getTipForCountryCode(nextProps.countryCode);
	        }
	    };
	
	    default_1.prototype.componentDidMount = function componentDidMount() {
	        this._getTipForCountryCode(this.props.countryCode.toUpperCase());
	    };
	
	    default_1.prototype.render = function render() {
	        var _this2 = this;
	
	        if (this.state.error) {
	            return React.createElement("p", null, this.state.error);
	        }
	        var rows = [];
	        Object.keys(this.state.tip).forEach(function (key) {
	            if (key != 'default') {
	                rows.push(React.createElement("tr", { key: key }, React.createElement("td", null, key), React.createElement("td", null, _this2.state.tip[key])));
	            }
	        });
	        return React.createElement("div", { style: {
	                display: 'flex',
	                alignItems: 'center',
	                justifyContent: 'center',
	                flexWrap: 'wrap'
	            } }, React.createElement("p", null, "In ", React.createElement("b", null, this.state.countryName), ", tipping is generally ", ' ', " ", React.createElement("b", null, this.state.tip.default), ".", rows.length > 0 && React.createElement("span", null, " Other guidelines:")), rows.length > 0 && React.createElement("table", { style: {
	                width: '50vh'
	            } }, React.createElement("tbody", null, rows)));
	    };
	
	    return default_1;
	}(React.Component);
	
	exports.default = default_1;

/***/ }),

/***/ 197:
/***/ (function(module, exports, __webpack_require__) {

	"use strict";
	
	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
	
	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }
	
	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }
	
	Object.defineProperty(exports, "__esModule", { value: true });
	var React = __webpack_require__(5);
	var suggestedTip_1 = __webpack_require__(194);
	var getCountryCode_1 = __webpack_require__(199);
	var queryString = __webpack_require__(317);
	
	var default_1 = function (_React$Component) {
	    _inherits(default_1, _React$Component);
	
	    function default_1(props) {
	        _classCallCheck(this, default_1);
	
	        var _this = _possibleConstructorReturn(this, _React$Component.call(this, props));
	
	        _this.defaultPositionOptions = {
	            enableHighAccuracy: false,
	            timeout: 10000,
	            maximumAge: 0
	        };
	        _this.state = { isLoaded: false };
	        return _this;
	    }
	
	    default_1.prototype._getCurrentPosition = function _getCurrentPosition() {
	        var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.defaultPositionOptions;
	
	        console.log("Retrieving current geolocation.");
	        return new Promise(function (resolve, reject) {
	            if (!navigator || !navigator.geolocation) {
	                reject(new Error('Geolocation: Not Supported'));
	            }
	            navigator.geolocation.getCurrentPosition(function (pos) {
	                return resolve(pos.coords);
	            }, function () {
	                return reject(new Error('Geolocation: Permission denied'));
	            }, options);
	        });
	    };
	
	    default_1.prototype.componentDidMount = function componentDidMount() {
	        var _this2 = this;
	
	        var countryCode = queryString.parse(this.props.location.search).countryCode;
	        if (countryCode) {
	            this.setState({ isLoaded: true, countryCode: countryCode });
	            return;
	        }
	        this._getCurrentPosition().then(function (position) {
	            return getCountryCode_1.default(position);
	        }).then(function (location) {
	            _this2.setState({ isLoaded: true, countryCode: location.countryCode });
	        }).catch(function (err) {
	            var errMsg = 'Unable to retrieve current location.';
	            if (err instanceof Error) {
	                var error = err;
	                errMsg += " " + error.message;
	            }
	            console.error(errMsg);
	            _this2.setState({
	                isLoaded: true,
	                error: errMsg
	            });
	        });
	    };
	
	    default_1.prototype.render = function render() {
	        if (!this.state.isLoaded) {
	            return React.createElement("p", null, "Grabbing your location and local tip etiquette...");
	        }
	        if (this.state.error) {
	            return React.createElement("p", null, this.state.error);
	        }
	        return React.createElement(suggestedTip_1.default, { countryCode: this.state.countryCode });
	    };
	
	    return default_1;
	}(React.Component);
	
	exports.default = default_1;

/***/ }),

/***/ 198:
/***/ (function(module, exports) {

	"use strict";
	
	Object.defineProperty(exports, "__esModule", { value: true });
	function default_1(url) {
	    var timeout = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 5000;
	
	    return Promise.race([fetch(url).then(function (response) {
	        return response.json();
	    }), new Promise(function (_, reject) {
	        return setTimeout(function () {
	            return reject(new Error('timeout'));
	        }, timeout);
	    })]);
	}
	exports.default = default_1;

/***/ }),

/***/ 199:
/***/ (function(module, exports, __webpack_require__) {

	"use strict";
	
	Object.defineProperty(exports, "__esModule", { value: true });
	var fetchWithTimeout_1 = __webpack_require__(198);
	var geocode = {
	    baseUrl: 'http://api.geonames.org/countryCodeJSON',
	    apiKey: 'jasonpoon'
	};
	function default_1(coordinates) {
	    console.log("Geocoding lat=" + coordinates.latitude + ", lng=" + coordinates.longitude + ".");
	    var url = geocode.baseUrl + "?lat=" + coordinates.latitude + "&lng=" + coordinates.longitude + "&username=" + geocode.apiKey;
	    return fetchWithTimeout_1.default(url);
	}
	exports.default = default_1;

/***/ })

});
//# sourceMappingURL=component---src-pages-index-tsx-1ef688af6776ec495918.js.map